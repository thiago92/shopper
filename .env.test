# Configurações específicas para ambiente de teste
NODE_ENV=test

# Chave mockada - nunca use chaves reais em testes
GEMINI_API_KEY=mock_key_12345

# Banco de dados de teste (deve ser diferente do produção)
PORT=3000
DB_HOST=localhost
DB_USER=test_user
DB_PASSWORD=test_password
DB_NAME=test_water_gas_db

# Configurações adicionais para evitar efeitos colaterais
DEBUG=false
LOG_LEVEL=error